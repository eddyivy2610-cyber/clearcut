services:
  - type: web
    name: clear-cut
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: UNSPLASH_ACCESS_KEY
        sync: false
